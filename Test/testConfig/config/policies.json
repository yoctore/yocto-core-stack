{
    "policies" : {
        "app.name"                    : "string",
        "app.adminTimeOut"            : "number",
        "db.name"                     : "string",
        "db.username"                 : "string",
        "db.port"                     : "number",
        "db.host"                     : "string",
        "mailer.port"                 : "number",
        "mailer.host"                 : "string",
        "mailer.adminAddress"         : "string",
        "express.redirect.vhost.port" : "number",
        "express.vhost.subdomains"    : "boolean"
    }
}
